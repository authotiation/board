<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.human.board.mapper.BoardFileMapper">

    <select id="selectByBoardId" parameterType="Long" resultType="BoardFileVo">
        SELECT id, board_id, original_name, saved_name, file_path, file_size, created_at, deleted
        FROM board_file
        WHERE board_id = #{boardId} AND deleted = 1
    </select>

    <select id="selectOne" parameterType="Long" resultType="BoardFileVo">
        SELECT id, board_id, original_name, saved_name, file_path, file_size, created_at, deleted
        FROM board_file
        WHERE id = #{id} AND deleted = 1
    </select>

    <insert id="insert" parameterType="BoardFileVo" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO board_file (board_id, original_name, saved_name, file_path, file_size)
        VALUES (#{boardId}, #{originalName}, #{savedName}, #{filePath}, #{fileSize})
    </insert>

    <!-- soft delete 단건 -->
    <update id="delete" parameterType="Long">
        UPDATE board_file
        SET deleted = 0
        WHERE id = #{id}
    </update>

    <!-- soft delete 게시글 전체 파일 -->
    <update id="deleteByBoardId" parameterType="Long">
        UPDATE board_file
        SET deleted = 0
        WHERE board_id = #{boardId}
    </update>

</mapper>
